[tool.poetry]
name = "content-ci-cd-template"
version = "0.1.0"
description = ""
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.8,<3.11"
demisto-sdk = "*"
virtualenv = "^20.25.0"
jsonschema = "^4.21.1"
gsutil = "^5.27"

[tool.poetry.group.azure-upload.dependencies]
azure-storage-blob = "^12.0"
prettytable = "^3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
